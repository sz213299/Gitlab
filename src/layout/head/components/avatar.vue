<template>

<el-dropdown>
    <span class="el-dropdown-link">
        <el-avatar shape="square" :size="40" :src="squareUrl" />、
        &nbsp; &nbsp;{{ currenUser.name }}
      <el-icon class="el-icon--right">
        <arrow-down />
      </el-icon>
    </span>
    <template #dropdown>
      <el-dropdown-menu>
        <el-dropdown-item>个人中心</el-dropdown-item>
        <el-dropdown-item @click="logout">安全退出</el-dropdown-item>
      </el-dropdown-menu>
    </template>
  </el-dropdown>
  </template>
  
  <script setup>

import router from '@/router'
import requsetUtil ,{getServerUrl} from '@/util/request'
import store from '@/store';

import { ArrowDown } from '@element-plus/icons-vue'

const currenUser=JSON.parse(sessionStorage.getItem('currenUser'))
const squareUrl=getServerUrl()+'media/userAvatar/'+currenUser.avatar
// 关闭绑定
const logout=()=>{
  window.sessionStorage.clear()
  store.commit('RESET_TAB')
  router.replace('/')
}

  
  </script>
  
  <style lang="scss" scoped>
.el-dropdown-link {
 cursor: pointer;
 color: var(--el-color-primary);
 display: flex;
 align-items: center;
}
  
  </style>